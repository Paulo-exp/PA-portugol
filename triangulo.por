programa {
  funcao inicio() {
    real base, altura, total

    escreva("Digite a base : ")
    leia(base)
    escreva("Digite a altura : ")
    leia(altura)

    total = (base*altura) / 2

    escreva("O resultado é: ", total)
    
  
  }
}
